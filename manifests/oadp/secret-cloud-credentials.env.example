# Copy to cloud-credentials-velero Secret (key: cloud) — do not commit real keys.
# MinIO default user from kcli plan VM: minioadmin / minioadmin-change-me (change immediately).
#
[default]
aws_access_key_id=minioadmin
aws_secret_access_key=REPLACE_ME
